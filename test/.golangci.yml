run:
  deadline: 3m

  # exclude test files
  tests: false

  # assume downloaded modules
  modules-download-mode: readonly

  # skip generated code
  skip-dirs:
    - assets

  skip-files:
    - ".*assets.go"